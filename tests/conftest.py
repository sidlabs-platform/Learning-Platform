"""Shared pytest fixtures for the Learning Platform test suite.

This module is automatically loaded by pytest for every test session.
Add reusable fixtures here as services and endpoints are implemented in later waves.
"""

import pytest


# ---------------------------------------------------------------------------
# Placeholder fixtures — populated in subsequent waves
# ---------------------------------------------------------------------------


@pytest.fixture
def placeholder() -> None:
    """Placeholder fixture — replace with real shared fixtures in later waves."""
    return None
